[project]
name = "leafgreen_bot"
version = "0.0.1"
# Other Python version have not been tested.
requires-python = "=3.14"
dependencies = [
    "dotenv~=0.9.9"
]

[project.optional-dependencies]
dev = [
    "pytest~=9.0.2",
]
